#!/usr/bin/env bash
# ArchCosta Installer Launcher
# Starts ArchCosta Installer with proper privileges

if [[ $EUID -ne 0 ]]; then
    pkexec archcosta-installer "$@"
else
    archcosta-installer "$@"
fi
